
export default function DaysUntilStress(deficitVals) {
    
  //console.log(deficitVals[deficitVals.length - 1]);

  var slope = deficitVals[deficitVals.length - 1] - deficitVals[deficitVals.length - 2];

  

  return(0);

}